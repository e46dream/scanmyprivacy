{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "nixpacksPlan": {
      "phases": {
        "setup": {
          "nixPkgs": ["nodejs_22"]
        },
        "install": {
          "cmds": ["npm ci"]
        },
        "build": {
          "cmds": ["npx playwright install chromium && npx playwright install-deps chromium"]
        }
      }
    }
  },
  "deploy": {
    "startCommand": "pwd && ls -la && ls -la node_modules 2>/dev/null | head -5 || echo 'NO NODE_MODULES' && npm install && node api/server-simple.js",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
